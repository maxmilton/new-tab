/**
 * Empty background script
 *
 * @fileoverview This file is intentionally empty; it's used to force the
 * extension to remain in memory for faster new-tab page open (at the cost of
 * memory use even when the new-tab page is closed)
 */

export {};
